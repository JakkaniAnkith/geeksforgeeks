class Solution:

    def firstRepChar(self, s):
        # code here
        d={}
        for i in s:
            if i in d:
                d[i]+=1
                return i
            else:
                d[i]=1
        return -1
